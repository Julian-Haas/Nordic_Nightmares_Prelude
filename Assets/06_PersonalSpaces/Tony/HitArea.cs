using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HitArea : MonoBehaviour {
    /*

    public Action<Vector3> TriggerHit;
    public Action TriggerLeft;

    private void OnTriggerEnter(Collider other) {
        if (!GlobalData.Instance.Player.CheckIsHidden(transform.parent.gameObject)) {
            Debug.Log("hit");
            Debug.Log(other.transform.position);
            TriggerHit?.Invoke(other.transform.position);
        }

    }
    private void OnTriggerExit(Collider other) {
        TriggerLeft?.Invoke();
    }
     */
}
