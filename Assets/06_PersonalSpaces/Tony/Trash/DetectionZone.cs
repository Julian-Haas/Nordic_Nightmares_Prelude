using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DetectionZone : MonoBehaviour
{
    /*
    public Naddi naddi;

    void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.tag == "Player" && !GlobalData.Instance.Player.CheckIsHidden(transform.parent.gameObject))
        {
            naddi.PlayerInView();
        }
    }
    void OnTriggerExit(Collider other)
    {
        if (other.gameObject.tag == "Player" || GlobalData.Instance.Player.CheckIsHidden(transform.parent.gameObject))
        {
            naddi.PlayerOutOfView();
        }
    }
     */
}